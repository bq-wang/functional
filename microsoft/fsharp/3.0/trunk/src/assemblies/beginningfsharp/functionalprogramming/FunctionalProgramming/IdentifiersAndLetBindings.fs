module IdentifiersAndLetBindings

let x = 42

// you can have apostraphe (whic is not commonly found in many )

let x' = 43

// unicode is supported, you can use accented characters and letters from non-Latin alphabets
let 标识符 = 42


// and doule tick marks (``) give you more flexibility 
let ``more?`` = true

let ``class`` = "style"